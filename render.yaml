services:
  - type: web
    name: visionshield-api
    runtime: docker
    rootDir: backend
    dockerfilePath: ./Dockerfile
    plan: free
    healthCheckPath: /health
    envVars:
      # Leave model paths empty until the real checkpoints are available.
      # The backend intentionally reports the models as unavailable rather than fabricating inference results.
      - key: NAFNET_ITS_WEIGHTS_PATH
        value: ""
      - key: NAFNET_OTS_WEIGHTS_PATH
        value: ""
      - key: NAFNET_RAIN_WEIGHTS_PATH
        value: ""
      - key: NAFNET_SNOW_WEIGHTS_PATH
        value: ""
      - key: NAFNET_LOWLIGHT_WEIGHTS_PATH
        value: ""
      - key: RTDETR_WEIGHTS_PATH
        value: ""
      - key: CORS_ORIGINS
        fromService:
          name: visionshield-frontend
          type: web
          envVarKey: RENDER_EXTERNAL_URL
      - key: ARTIFACT_DIR
        value: /tmp/visionshield-artifacts
      - key: DATABASE_PATH
        value: /tmp/visionshield.db

  - type: web
    name: visionshield-frontend
    runtime: static
    rootDir: frontend
    buildCommand: npm ci && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_API_URL
        fromService:
          name: visionshield-api
          type: web
          envVarKey: RENDER_EXTERNAL_URL
