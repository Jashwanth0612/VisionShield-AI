# RT-DETR checkpoint

Place your project-specific RT-DETR checkpoint here as:

`rtdetr_weights.pt`

If it is absent, the backend automatically uses the public pretrained `rtdetr-l.pt` checkpoint for demo inference.
