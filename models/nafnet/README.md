# NAFNet checkpoint

Place the project-compatible NAFNet checkpoint here as:

`nafnet_weights.pth`

The backend accepts a TorchScript module or serialized `torch.nn.Module`. The exact architecture must match the checkpoint produced by the training/research pipeline.
