# Required for production SEO output (canonical URLs, sitemap.xml and robots.txt).
# Set this to the real deployed origin or custom domain, with no trailing slash.
VITE_SITE_URL=https://your-real-domain.example
